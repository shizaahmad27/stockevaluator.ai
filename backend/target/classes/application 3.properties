# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/stockevaluator}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

########## HikariCP (the default connection pool) ##########
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.idle-timeout=600000

########## Logging ##########
logging.level.root=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.stockevaluator=DEBUG

########## Profiles ##########
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# JWT Configuration
# Access token: 30 minutes, Refresh token: 7 days, Reset password token: 15 minutes
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.access-token-expiration=1800000
jwt.refresh-token-expiration=604800000
jwt.reset-password-token-expiration=900000

# Server Configuration
server.port=${SERVER_PORT:8080}

# Frontend Configuration
frontend.url=${FRONTEND_URL:http://localhost:5173}
frontend.password-reset-path=/reset-password

# Reddit API Configuration
reddit.client.id=${REDDIT_CLIENT_ID}
reddit.client.secret=${REDDIT_CLIENT_SECRET}
reddit.username=${REDDIT_USERNAME}
reddit.password=${REDDIT_PASSWORD}
